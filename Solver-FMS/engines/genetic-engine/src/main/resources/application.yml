spring:
  application:
    name: genetic-engine

server:
  port: 8093

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
