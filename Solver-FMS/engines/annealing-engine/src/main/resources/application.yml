spring:
  application:
    name: annealing-engine

server:
  port: 8094

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
