"""
Analizar: aprender de las mediciones que solver conviene segun la instancia. Lo usa
decision_tree.py.

  dataset.py    El cruce de las dos tablas y como se decide que solver conviene
  model.py      Entrenar el arbol, validarlo y verificar el algoritmo
  plot.py       El arbol como imagen
  report.py     El informe en Markdown

Parte de los CSV ya escritos por `comparison`, asi que no habla con la pasarela ni necesita
el sistema levantado. A cambio depende de pandas, scikit-learn y matplotlib:

    pip install pandas scikit-learn matplotlib
"""
